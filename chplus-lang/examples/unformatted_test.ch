// 这是一个未格式化的.ch代码示例，包含中文标点符号
定义(空类型) 主函数() {
    控制台输出("Hello World");
    定义(整型) x = 10;
    如果(x > 5) {
        控制台输出("x大于5");
    } 否则 {
        控制台输出("x小于等于5");
    }
    // 循环示例
    对于(定义(整型) i = 0; i < 3; i = i + 1) {
        控制台输出("数字:" + i);
    }
}