// 全局作用域：只能定义变量和结构体
定义(整型) globalVar = 100;

定义(结构体) GlobalStruct {
    整型 value;
};

定义(空类型) 主函数() {
    // 函数内部可以执行各种操作
    控制台输出("在函数内部可以执行各种操作");
    控制台输出("全局变量值: " + globalVar);
    
    // 函数内定义的变量
    定义(整型) localVar = 50;
    控制台输出("局部变量值: " + localVar);
    
    // 文件操作（只在函数内允许）
    文件写入("test.txt", "这是函数内的操作");
}