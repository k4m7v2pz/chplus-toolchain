// 最简结构体数组测试
定义(结构体) Point {
    整型 x;
    整型 y;
};

定义(空类型) 主函数() {
    控制台输出("最简结构体数组测试");
    
    // 仅定义结构体数组
    定义(Point) points[3];
    
    控制台输出("结构体数组定义成功！");
    控制台输出("语法定义(结构体名称) a[1000] 支持正常！");
    控制台输出("无需赋值即可测试语法正确性");
}