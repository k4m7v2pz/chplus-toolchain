定义(空类型) 主函数() {
    定义(整型) score = 85;
    
    如果 (score >= 90) {
        控制台输出("成绩优秀");
    }否则如果 (score >= 80) {
        控制台输出("成绩良好");
    }否则如果 (score >= 70) {
        控制台输出("成绩中等");
    }否则如果 (score >= 60) {
        控制台输出("成绩及格");
    }否则 {
        控制台输出("成绩不及格");
    }
}