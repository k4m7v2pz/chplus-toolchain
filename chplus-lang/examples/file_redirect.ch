导入("ch_Lib/file.ch");

定义(空类型) 主函数() {
    // 文件重定向
    重定向标准输出("log.txt");
    控制台输出("这条信息写入文件");
    恢复标准输出();
    
    // 文件操作
    如果 (文件存在("config.txt")) {
        定义(字符串) content = 读取文件("config.txt");
        控制台输出("配置文件内容: " + content);
    }
}